glob='java-digital-notary-providers-*-exec.jar'
jar=($glob)
[[ -e "$jar" ]] || jar=(target/$glob)
[[ -e "$jar" ]] || {
  echo "can't find $glob in . or target" 2>&1
  exit 1
}

java -jar $jar "$@"

exit $?
